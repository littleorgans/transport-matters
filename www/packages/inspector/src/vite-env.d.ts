/// <reference types="vite/client" />

// Injected by the building Vite config's `define` (the shell while the
// products compose into one bundle; the inspector's own config once it
// ships separately). See shell/vite.config.ts.
declare const __TRANSPORT_MATTERS_VERSION__: string;
