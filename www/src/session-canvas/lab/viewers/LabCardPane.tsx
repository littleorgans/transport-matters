// Stub content. The lab proves the LAYOUT registry, not a content registry — content is
// deliberately trivial and layout-agnostic.
export function LabCardPane({ paneId }: { paneId: string }) {
  return <div className="canvas-stress-card">Pane {paneId}</div>;
}
