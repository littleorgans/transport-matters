{
  "name": "transport-matters-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-channel-specs.mjs && node scripts/assert-preload-cjs.mjs",
    "clean": "rimraf dist node_modules",
    "dev": "pnpm build && pnpm electron:install && electron .",
    "electron:install": "node node_modules/electron/install.js",
    "package:smoke": "pnpm build && pnpm electron:install && node scripts/package-smoke-build.mjs && node dist/packageSmoke.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "electron": "^39.2.6"
  }
}
