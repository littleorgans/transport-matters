set shell := ["bash", "-cu"]

default: check

test:
    pnpm test

typecheck:
    pnpm typecheck

build:
    pnpm build

dev:
    pnpm dev

package-smoke:
    pnpm package:smoke

check: typecheck test

clean:
    pnpm clean
